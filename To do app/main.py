# create Task app
# front end to add a task, displays the task added, able to delete a task.
# task has a description, date
# create an Api
# connect to a database (internal port 8000)
from tkinter import *

window = Tk()
window.title("Task app")
window.config(padx=50, pady=50)
# create canvas
canvas = Canvas(width=800, height=526)

window.mainloop()
